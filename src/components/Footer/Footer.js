const Footer = ({ list, setList }) => {
  console.log(list);
  return (
    <footer className="bg-dark">
      <h2 className="text-center text-white">Soy un footer</h2>
    </footer>
  );
}
 
export default Footer;